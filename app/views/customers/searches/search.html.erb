<div class="container px-5 px-sm-0">
  <div class="row">
    <div class="col-lg-9 mx-auto">
      <div class="card shadow-lg px-3 py-3 text-center">
        <h2>検索一覧</h2>
      </div>
      <div class="card shadow-lg px-3 py-3">
        <% if @customers.empty? %>
          <p>ユーザーの検索結果は見つかりませんでした。</p>
        <% else %>
          <% @customers.each do |customer| %>
            <% unless customer == current_customer %>
              <%= render 'customers/customers/customers_index', customer: customer %>
            <% end %>
          <% end %>
        <% end %>
      </div>
      <div class="card shadow-lg px-3 py-3">
        <% if @posts.empty? %>
          <p>投稿の検索結果は見つかりませんでした。</p>
        <% else %>
          <% @posts.each do |post| %>
            <% unless @customer == current_customer %>
              <%= render 'customers/posts/posts_list' %>
            <% end %>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
</div>